services:
  mastering-backend:
    root: backend
    buildCommand: pip install -r requirements.txt && python3 -m pip install uvicorn fastapi
    startCommand: python3 -m uvicorn mastering_api:app --host 0.0.0.0 --port $PORT
    envVars:
      PORT: 8000
      PYTHONUNBUFFERED: 1
